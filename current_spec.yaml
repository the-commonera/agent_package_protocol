name: pr_reviewer
version: 1.0.0
author: Platform

runtime:
  provider: provider value
  model: modelname

inputs:
  query:
    type: string
    required: true
    description: description of the input

prompt: |
 prompt here 


tools:
  - type: mcp
    url: 


secrets:
  SOME_TOKEN:
    required: true
    description:  token.

outputs:
  data:
    review:
      type: string
      description: description of the output
