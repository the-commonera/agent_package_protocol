name: pr_reviewer
version: 1.0.0
author: Platform

runtime:
  provider: provider value
  model: modelname
  temperature: temp
  top_p: top p value
  

inputs:
  query:
    type: string
    required: true
    description: description of the input

prompt: |
    - type :
      url:

constraints : |

  constraints here

skills :

/////////////////////////////////// DEFINED BY THE AUTHOR EXAMPLE

  redteam : 
   - type :
      url:
 /////////////////////////////////////// 
tools:
  - type: mcp
    url: 


resources:
    - type: 
      url : 

secrets:
  SOME_TOKEN:
    required: true
    description:  token.

outputs:
  data:
    review:
      type: string
      description: description of the output
